float a = 0, b = 1, c = 3;
int d = 10;

print foo(5.12);
print bar(3 + 8, "abc");

print baz(3 != 5, "abc" * how_many, Whatchamacallit);

print qux(hello);

while ( a > 1 ) 
	print baba(2);

while ( c >= 1 ) {
	break;
	print "compound instructions";
}
	
return b(a);

a = b + c;

label_1 : a = p;
label_2 : print "w sumie to jak wypisywac labele?";
